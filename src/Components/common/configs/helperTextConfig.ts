import { IPropConfig } from "rx-drag/models/IPropConfig";

const helperTextConfig:IPropConfig ={
  name:'helperText',
  labelKey:'helper-text',

  propType:'string',
  props:{
    xs:12
  }
}

export default helperTextConfig;